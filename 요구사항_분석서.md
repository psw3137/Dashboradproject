# 매출/이용 패턴 통계 대시보드 - 요구사항 분석서

**프로젝트명**: 매출/이용 패턴 통계 대시보드  
**작성일**: 2024년  
**팀 구성**: 웹프레임워크백엔드 2조  
**대상**: 스크린 골프장 관리자용 데이터 분석 시스템

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [데이터 분석 및 정제 계획](#2-데이터-분석-및-정제-계획)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [기능 요구사항](#4-기능-요구사항)
5. [비기능 요구사항](#5-비기능-요구사항)
6. [개발 일정](#6-개발-일정)
7. [역할 분담](#7-역할-분담)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 배경
- 스크린 골프장의 22,478명 고객 데이터 (5월 기준) 보유
- 총 매출 약 13억원의 데이터를 효과적으로 분석할 필요성
- 관리자가 경영 현황을 한눈에 파악하고 의사결정을 지원하는 시스템 필요

### 1.2 프로젝트 목표
1. **데이터 시각화**: 매출·이용 패턴을 직관적인 차트로 표현
2. **정보 조회**: 고객별, 지역별, 연령대별 상세 정보 제공
3. **의사결정 지원**: KPI 지표를 통한 경영 현황 모니터링

### 1.3 주요 사용자
- **관리자**: 매출 분석, 경영 판단
- **마케팅 담당자**: 타깃 고객 분석, 프로모션 기획

### 1.4 시스템 제약사항
- 학부 프로젝트 수준의 구현
- 웹 기반 애플리케이션 (모바일 반응형)
- 5월 데이터 기준의 정적 분석 (실시간 업데이트는 향후 과제)

---

## 2. 데이터 분석 및 정제 계획

### 2.1 데이터셋 개요

#### 기본 정보
```
- 총 레코드 수: 22,478개
- 컬럼 수: 14개
- 기간: 5월 기준 데이터
- 파일 형식: CSV
```

#### 컬럼 구조
| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| uid | Integer | 고객 고유 ID | 11, 705, 1033 |
| region_city_group | String | 광역시도 | Seoul, Gyeonggi-do |
| region_city_group_no | Integer | 지역 그룹 번호 | 1, 2, 3 |
| region_city | String | 시/군/구 | Seoul, Seongnam, Yongin |
| age_group | String | 연령대 | Teens, Twenties, Thirties, Forties+ |
| age | Integer | 나이 | 10~60 |
| visit_days | Integer | 5월 방문 일수 | 1~31 |
| total_duration_min | Integer | 총 이용 시간(분) | 0~25,849 |
| avg_duration_min | Integer | 평균 이용 시간(분) | total_duration_min / visit_days |
| total_payment_may | Integer | 5월 총 결제 금액(원) | 0~1,116,000 |
| retained_june | Binary | 6월 재방문 여부 | 0 or 1 |
| retained_july | Binary | 7월 재방문 여부 | 0 or 1 |
| retained_august | Binary | 8월 재방문 여부 | 0 or 1 |
| retained_90 | Binary | 90일 이내 재방문 여부 | 0 or 1 |

### 2.2 데이터 품질 분석 결과

#### ✅ 양호한 부분
- 중복 데이터 없음
- 결측치(NULL) 없음
- 계산 필드(avg_duration_min) 정확성 확인
- 유지율 로직 일관성 확인

#### ⚠️ 정제 필요 사항

**1) 지역명 영문 → 한글 변환**
```
문제: region_city가 영문으로 되어 있음
예시: Seoul, Yongin, Cheonan, Seongnam 등

해결: 매핑 테이블을 통한 한글 변환
Seoul → 서울
Yongin → 용인
Seongnam → 성남
등
```

**2) 매출 0원 고객 처리**
```
현황: 316명 (1.41%)이 방문은 했으나 결제 0원
원인: 무료 체험, 쿠폰 사용, 데이터 누락 등
처리: 분석 시 제외 옵션 제공 (선택적 필터링)
```

**3) 이상치(Outlier) 처리**
```
극단적 매출:
- 50만원 이상: 348명
- 최대: 1,116,000원
→ 단체 이용, VIP 고객 등으로 판단, 유지

극단적 방문:
- 25일 이상: 260명
- 최대: 31일 (한 달 매일)
→ 정기 회원으로 판단, 유지

극단적 이용시간:
- 10,000분 이상: 523명
- 최대: 25,849분 (430시간)
→ 누적 시간 오류 가능성, 검토 필요
```

### 2.3 데이터 정제 프로세스

```python
# 데이터 정제 단계

1단계: 데이터 로드 및 검증
   - CSV 파일 읽기
   - 컬럼 타입 확인
   - 기본 통계 확인

2단계: 지역명 한글 변환
   - 매핑 딕셔너리 생성
   - region_city 변환
   - 변환 결과 검증

3단계: 파생 변수 생성
   - 객단가 = total_payment_may / visit_days
   - 1회 평균 결제금액
   - 고객 등급 (Low/Mid/High spender)

4단계: 카테고리 정리
   - 지역 그룹 재분류 (필요시)
   - 연령대 한글화 (선택)

5단계: 정제된 데이터 저장
   - MongoDB에 저장
   - JSON 형식 변환
   - 인덱스 생성
```

### 2.4 주요 통계 (정제 전 데이터 기준)

```
📊 전체 통계
- 총 고객 수: 22,478명
- 총 매출: 1,331,828,231원 (약 13.3억)
- 평균 객단가: 59,250원
- 중앙값: 17,000원

👥 고객 분포
- 10대: 49.8% (11,196명)
- 20대: 34.4% (7,723명)
- 30대: 8.5% (1,911명)
- 40대 이상: 7.3% (1,648명)

🗺️ 지역 분포
- 서울: 36.0% (8,082명, 매출 4.4억)
- 경기: 25.7% (5,779명)
- 전북: 6.2% (1,401명)
- 인천: 4.2% (938명, 매출 0.9억)

📈 이용 패턴
- 평균 방문: 4.24일
- 중앙값 방문: 2일
- 1일만 방문: 43.56% (9,791명)
- 평균 이용시간: 234분 (약 4시간)

💰 매출 분포
- 1만원 이하: 36.68%
- 1~5만원: 32.26%
- 5~10만원: 12.96%
- 10만원 이상: 16.69%

🔄 고객 유지율
- 6월 재방문: 24.17%
- 7월 재방문: 27.61%
- 8월 재방문: 23.87%
- 90일 내 재방문: 46.26%
```

---

## 3. 시스템 아키텍처

### 3.1 기술 스택

```
Frontend
├── React Native (또는 React.js)
├── Chart.js / Recharts (그래프 라이브러리)
├── Axios (HTTP 클라이언트)
└── Tailwind CSS / Bootstrap (스타일링)

Backend
├── Node.js + Express.js
├── Mongoose (MongoDB ODM)
├── CORS (Cross-Origin 처리)
└── dotenv (환경변수 관리)

Database
└── MongoDB
    ├── customers 컬렉션 (고객 데이터)
    └── statistics 컬렉션 (통계 캐시)

Tools
├── Git/GitHub (버전 관리)
├── Postman (API 테스트)
└── VS Code (개발 환경)
```

### 3.2 시스템 구조도

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Dashboard    │  │ Customer     │  │ Analytics    │  │
│  │ Page         │  │ List Page    │  │ Page         │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                            │
                            │ HTTP/REST API
                            │
┌─────────────────────────────────────────────────────────┐
│                      Backend Layer                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │              Express.js Server                    │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  • /api/statistics (통계 API)                     │  │
│  │  • /api/customers (고객 조회 API)                 │  │
│  │  • /api/charts (차트 데이터 API)                  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            │
                            │ Mongoose ODM
                            │
┌─────────────────────────────────────────────────────────┐
│                      Database Layer                      │
│                        MongoDB                           │
│  ┌────────────────────┐  ┌────────────────────┐        │
│  │ customers          │  │ statistics         │        │
│  │ (고객 데이터)        │  │ (통계 캐시)          │        │
│  └────────────────────┘  └────────────────────┘        │
└─────────────────────────────────────────────────────────┘
```

### 3.3 데이터 흐름

```
1. 초기 데이터 로드
   CSV → Node.js Script → MongoDB

2. 사용자 요청 흐름
   User → Frontend → API Request → Backend
   Backend → MongoDB Query → Data Processing
   Processed Data → API Response → Frontend → Chart Rendering

3. 필터링 흐름
   User selects filter → Frontend → API with query params
   Backend filters data → Return filtered result
```

---

## 4. 기능 요구사항

### 4.1 대시보드 (Dashboard) 기능

#### 4.1.1 월별 매출 추이 그래프 ⭐
**담당자**: 박선우

**기능 설명**
- 5월 일별 매출 추이를 선형 그래프로 시각화
- 매출 증감 추세를 파악하여 성수기/비수기 분석

**입력 데이터**
- `total_payment_may` (고객별 5월 총 결제금액)

**처리 로직**
```javascript
// 일별로 그룹화할 수 없으므로 고객별 누적 매출로 대체
// 또는 구간별(1~7일, 8~14일 등) 매출 합계

1. MongoDB에서 모든 고객 데이터 조회
2. 매출 데이터를 기간별로 그룹화
   - 방법1: visit_days를 기준으로 구간 나누기
   - 방법2: 고객 수 기준 누적 매출 계산
3. 그룹별 매출 합계 계산
4. JSON 형태로 프론트엔드에 반환
```

**출력 형태**
```json
{
  "labels": ["1주차", "2주차", "3주차", "4주차"],
  "data": [250000000, 280000000, 320000000, 481828231]
}
```

**차트 타입**: Line Chart (선형 그래프)

**구현 난이도**: ⭐⭐ (중하)

**검증 기준**
- [ ] 차트가 정상적으로 렌더링되는가?
- [ ] 총 매출 합계가 13.3억원과 일치하는가?
- [ ] 추세선이 명확히 보이는가?

---

#### 4.1.2 지역/연령대별 매출 분석 ⭐⭐
**담당자**: 박선우

**기능 설명**
- 지역별, 연령대별 매출 기여도를 시각화
- 어떤 세그먼트에서 매출이 집중되는지 파악
- 마케팅 타깃 설정에 활용

**입력 데이터**
- `region_city_group`, `region_city` (지역)
- `age_group` (연령대)
- `total_payment_may` (매출)
- `visit_days` (방문 일수)

**처리 로직**
```javascript
// 1) 지역별 매출
function getRevenueByRegion() {
  // MongoDB aggregation
  db.customers.aggregate([
    {
      $group: {
        _id: "$region_city_group",
        totalRevenue: { $sum: "$total_payment_may" },
        customerCount: { $sum: 1 },
        avgVisits: { $avg: "$visit_days" }
      }
    },
    { $sort: { totalRevenue: -1 } }
  ])
}

// 2) 연령대별 매출
function getRevenueByAge() {
  db.customers.aggregate([
    {
      $group: {
        _id: "$age_group",
        totalRevenue: { $sum: "$total_payment_may" },
        customerCount: { $sum: 1 }
      }
    }
  ])
}

// 3) 지역 x 연령대 교차 분석
function getRevenueByRegionAndAge() {
  db.customers.aggregate([
    {
      $group: {
        _id: {
          region: "$region_city_group",
          ageGroup: "$age_group"
        },
        totalRevenue: { $sum: "$total_payment_may" }
      }
    }
  ])
}
```

**출력 형태**
```json
{
  "byRegion": [
    { "region": "서울", "revenue": 441716600, "customers": 8082 },
    { "region": "경기", "revenue": 350000000, "customers": 5779 }
  ],
  "byAgeGroup": [
    { "ageGroup": "10대", "revenue": 500000000, "customers": 11196 },
    { "ageGroup": "20대", "revenue": 450000000, "customers": 7723 }
  ],
  "heatmap": [
    { "region": "서울", "ageGroup": "10대", "revenue": 150000000 },
    { "region": "서울", "ageGroup": "20대", "revenue": 200000000 }
  ]
}
```

**차트 타입**
- 지역별: Horizontal Bar Chart (가로 막대)
- 연령대별: Bar Chart (세로 막대)
- 교차 분석: Heatmap (히트맵)

**구현 난이도**: ⭐⭐⭐ (중)

**검증 기준**
- [ ] 각 그룹의 매출 합계가 전체 매출과 일치하는가?
- [ ] 상위 3개 지역이 명확히 표시되는가?
- [ ] 히트맵 색상이 매출 크기를 잘 반영하는가?

---

#### 4.1.3 고객 이용 비율 그래프 ⭐
**담당자**: 송재경

**기능 설명**
- 지역별 고객 수 비율을 파이 차트로 표현
- 전체 고객 분포를 한눈에 파악
- 지역별 고객 집중도 분석

**입력 데이터**
- `region_city_group` (광역 지역)
- `uid` (고객 ID)

**처리 로직**
```javascript
function getCustomerDistribution() {
  // 1. 지역별 고객 수 집계
  const result = db.customers.aggregate([
    {
      $group: {
        _id: "$region_city_group",
        count: { $sum: 1 }
      }
    },
    { $sort: { count: -1 } }
  ])
  
  // 2. 비율 계산
  const total = 22478
  return result.map(item => ({
    region: item._id,
    count: item.count,
    percentage: (item.count / total * 100).toFixed(2)
  }))
}
```

**출력 형태**
```json
[
  { "region": "서울", "count": 8082, "percentage": 35.96 },
  { "region": "경기도", "count": 5779, "percentage": 25.71 },
  { "region": "전라북도", "count": 1401, "percentage": 6.23 },
  { "region": "기타", "count": 7216, "percentage": 32.10 }
]
```

**차트 타입**: Pie Chart (파이 차트) 또는 Doughnut Chart

**추가 기능**
- 호버 시 상세 정보 표시 (고객 수, 비율)
- 클릭 시 해당 지역의 상세 정보 페이지로 이동 (선택)

**구현 난이도**: ⭐⭐ (하)

**검증 기준**
- [ ] 모든 비율의 합이 100%인가?
- [ ] 상위 5개 지역이 명확히 구분되는가?
- [ ] 차트가 반응형으로 작동하는가?

---

### 4.2 이용 현황 조회 기능

#### 4.2.1 KPI 지표 확인 ⭐⭐
**담당자**: 윤보석

**기능 설명**
- 핵심 성과지표(KPI)를 대시보드 상단에 표시
- 실시간(정적) 경영 현황 모니터링
- 주요 메트릭을 큰 숫자로 강조 표시

**KPI 목록**

1. **총 매출 (Total Revenue)**
   ```javascript
   총 매출 = SUM(total_payment_may)
   ```

2. **총 고객 수 (Total Customers)**
   ```javascript
   총 고객 수 = COUNT(DISTINCT uid)
   ```

3. **평균 객단가 (Average Revenue Per User, ARPU)**
   ```javascript
   평균 객단가 = 총 매출 / 총 고객 수
   ```

4. **평균 방문 일수 (Average Visits)**
   ```javascript
   평균 방문 = AVG(visit_days)
   ```

5. **고객 유지율 (Retention Rate - 90일)**
   ```javascript
   유지율 = COUNT(retained_90 = 1) / 총 고객 수 * 100
   ```

6. **활성 고객 비율 (Active Customer Rate)**
   ```javascript
   활성 고객 = COUNT(visit_days >= 5) / 총 고객 수 * 100
   ```

**처리 로직**
```javascript
async function getKPIMetrics() {
  const totalRevenue = await db.customers.aggregate([
    { $group: { _id: null, total: { $sum: "$total_payment_may" } } }
  ])
  
  const totalCustomers = await db.customers.countDocuments()
  
  const avgVisits = await db.customers.aggregate([
    { $group: { _id: null, avg: { $avg: "$visit_days" } } }
  ])
  
  const retentionRate = await db.customers.aggregate([
    { 
      $group: { 
        _id: null, 
        retained: { $sum: "$retained_90" },
        total: { $sum: 1 }
      } 
    },
    {
      $project: {
        rate: { $multiply: [{ $divide: ["$retained", "$total"] }, 100] }
      }
    }
  ])
  
  return {
    totalRevenue: totalRevenue[0].total,
    totalCustomers: totalCustomers,
    arpu: totalRevenue[0].total / totalCustomers,
    avgVisits: avgVisits[0].avg,
    retentionRate: retentionRate[0].rate
  }
}
```

**출력 형태**
```json
{
  "totalRevenue": 1331828231,
  "totalCustomers": 22478,
  "arpu": 59250,
  "avgVisits": 4.24,
  "retentionRate90": 46.26,
  "activeCustomerRate": 22.4
}
```

**UI 표시 예시**
```
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   총 매출        │ │  총 고객 수       │ │  평균 객단가      │
│ 13.3억원         │ │  22,478명        │ │  59,250원       │
│ ↑ -             │ │  ↑ -            │ │  ↑ -           │
└─────────────────┘ └─────────────────┘ └─────────────────┘

┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ 평균 방문 일수    │ │  90일 유지율     │ │  활성 고객 비율   │
│   4.24일         │ │   46.26%        │ │   22.4%         │
│   ↑ -           │ │   ↑ -           │ │   ↑ -           │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

**구현 난이도**: ⭐⭐ (중하)

**검증 기준**
- [ ] 모든 KPI가 정확히 계산되는가?
- [ ] 숫자가 읽기 쉽게 포맷팅되는가? (천 단위 콤마)
- [ ] 카드 레이아웃이 반응형인가?

---

#### 4.2.2 고객별 결제·출석 이력 조회 ⭐⭐⭐
**담당자**: 이태석

**기능 설명**
- 개별 고객의 상세 정보 조회
- 고객별 이용 패턴 분석
- 충성 고객과 이탈 고객 구분

**입력 파라미터**
- `uid` (고객 ID) - 선택적
- `page` (페이지 번호)
- `limit` (페이지당 개수)

**처리 로직**
```javascript
// 1. 전체 고객 목록 조회 (페이지네이션)
async function getCustomerList(page = 1, limit = 50) {
  const skip = (page - 1) * limit
  
  const customers = await db.customers
    .find({})
    .skip(skip)
    .limit(limit)
    .sort({ total_payment_may: -1 }) // 매출 높은 순
    
  const total = await db.customers.countDocuments()
  
  return {
    data: customers,
    pagination: {
      page,
      limit,
      total,
      totalPages: Math.ceil(total / limit)
    }
  }
}

// 2. 특정 고객 상세 조회
async function getCustomerDetail(uid) {
  const customer = await db.customers.findOne({ uid })
  
  if (!customer) {
    throw new Error('Customer not found')
  }
  
  // 추가 계산
  return {
    ...customer,
    avgPaymentPerVisit: customer.total_payment_may / customer.visit_days,
    customerGrade: calculateGrade(customer.total_payment_may),
    isRetained: customer.retained_90 === 1,
    retentionMonths: [
      customer.retained_june && '6월',
      customer.retained_july && '7월',
      customer.retained_august && '8월'
    ].filter(Boolean)
  }
}

// 3. 고객 등급 계산
function calculateGrade(payment) {
  if (payment >= 200000) return 'VIP'
  if (payment >= 100000) return 'Gold'
  if (payment >= 50000) return 'Silver'
  return 'Bronze'
}
```

**출력 형태**
```json
{
  "uid": 6626,
  "region": "서울",
  "city": "서울",
  "ageGroup": "30대",
  "age": 38,
  "visitDays": 30,
  "totalDuration": 21171,
  "avgDuration": 706,
  "totalPayment": 914000,
  "avgPaymentPerVisit": 30467,
  "customerGrade": "VIP",
  "isRetained": true,
  "retentionMonths": ["6월", "7월"],
  "retainedJune": 1,
  "retainedJuly": 1,
  "retainedAugust": 0,
  "retained90": 1
}
```

**UI 표시 형태**
- **테이블 뷰**: 고객 목록 (페이지네이션)
- **상세 모달**: 클릭 시 상세 정보 팝업
- **정렬 기능**: 매출순, 방문순, 이름순

**검색 기능** (선택)
- UID로 검색
- 지역으로 검색
- 연령대로 검색

**구현 난이도**: ⭐⭐⭐ (중)

**검증 기준**
- [ ] 페이지네이션이 정상 작동하는가?
- [ ] 고객 상세 정보가 정확한가?
- [ ] 고객 등급이 올바르게 분류되는가?

---

#### 4.2.3 지역/연령대 필터링 조회 ⭐⭐⭐
**담당자**: 이태석

**기능 설명**
- 특정 조건으로 고객 데이터 필터링
- 세그먼트별 상세 분석 지원
- 복합 필터링 가능 (지역 AND 연령대)

**필터 옵션**
1. **지역 필터**
   - `region_city_group` (광역시도)
   - `region_city` (시/군/구)

2. **연령대 필터**
   - `age_group` (Teens, Twenties, Thirties, Forties+)
   - `age` 범위 (10~20, 21~30 등)

3. **매출 필터**
   - 최소 매출
   - 최대 매출

4. **방문 필터**
   - 최소 방문 일수
   - 최대 방문 일수

5. **유지율 필터**
   - 90일 유지 고객만
   - 이탈 고객만

**API 엔드포인트**
```
GET /api/customers/filter?
  region=서울&
  ageGroup=Twenties&
  minPayment=50000&
  maxPayment=200000&
  retained90=1
```

**처리 로직**
```javascript
async function filterCustomers(filters) {
  // 쿼리 빌더
  const query = {}
  
  if (filters.region) {
    query.region_city_group = filters.region
  }
  
  if (filters.city) {
    query.region_city = filters.city
  }
  
  if (filters.ageGroup) {
    query.age_group = filters.ageGroup
  }
  
  if (filters.minAge || filters.maxAge) {
    query.age = {}
    if (filters.minAge) query.age.$gte = filters.minAge
    if (filters.maxAge) query.age.$lte = filters.maxAge
  }
  
  if (filters.minPayment || filters.maxPayment) {
    query.total_payment_may = {}
    if (filters.minPayment) query.total_payment_may.$gte = filters.minPayment
    if (filters.maxPayment) query.total_payment_may.$lte = filters.maxPayment
  }
  
  if (filters.minVisits) {
    query.visit_days = { $gte: filters.minVisits }
  }
  
  if (filters.retained90 !== undefined) {
    query.retained_90 = filters.retained90
  }
  
  // 실행
  const customers = await db.customers.find(query)
  
  // 통계 계산
  const stats = {
    count: customers.length,
    totalRevenue: customers.reduce((sum, c) => sum + c.total_payment_may, 0),
    avgVisits: customers.reduce((sum, c) => sum + c.visit_days, 0) / customers.length,
    retentionRate: customers.filter(c => c.retained_90 === 1).length / customers.length * 100
  }
  
  return {
    filters,
    data: customers,
    statistics: stats
  }
}
```

**출력 형태**
```json
{
  "filters": {
    "region": "서울",
    "ageGroup": "Twenties",
    "minPayment": 50000
  },
  "data": [
    { "uid": 6983, "region": "서울", "age": 23, ... },
    { "uid": 7069, "region": "서울", "age": 28, ... }
  ],
  "statistics": {
    "count": 125,
    "totalRevenue": 15000000,
    "avgRevenue": 120000,
    "avgVisits": 5.2,
    "retentionRate": 52.8
  }
}
```

**UI 구성**
```
┌────────────────────────────────────────────────────┐
│                 필터링 옵션                         │
├────────────────────────────────────────────────────┤
│ 지역: [서울 ▼]  연령대: [20대 ▼]                   │
│ 매출: [50,000원] ~ [200,000원]                     │
│ 방문: [5일] 이상                                    │
│ □ 90일 유지 고객만                                  │
│                               [필터 적용] [초기화]  │
└────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────┐
│              필터링 결과 (125명)                     │
├────────────────────────────────────────────────────┤
│ • 총 매출: 15,000,000원                            │
│ • 평균 매출: 120,000원                             │
│ • 평균 방문: 5.2일                                 │
│ • 유지율: 52.8%                                    │
└────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────┐
│                  고객 목록                          │
├─────┬──────┬────┬──────┬────────┬─────────────┤
│UID  │ 지역  │연령│방문일 │ 매출    │ 유지 여부    │
├─────┼──────┼────┼──────┼────────┼─────────────┤
│6983 │서울   │23  │ 4   │ 31,500 │ ×          │
│7069 │서울   │28  │ 5   │160,000 │ ○          │
└─────┴──────┴────┴──────┴────────┴─────────────┘
```

**구현 난이도**: ⭐⭐⭐⭐ (중상)

**검증 기준**
- [ ] 모든 필터 조합이 정상 작동하는가?
- [ ] 필터링 결과 통계가 정확한가?
- [ ] 빈 결과에 대한 처리가 되어 있는가?
- [ ] 필터 초기화 기능이 작동하는가?

---

### 4.3 공통 기능

#### 4.3.1 데이터 내보내기 (선택)
**담당자**: 공통

**기능 설명**
- 현재 표시된 데이터를 CSV 또는 Excel로 내보내기
- 관리자가 외부 분석 도구에서 사용 가능

**구현 방법**
```javascript
// Backend
const { Parser } = require('json2csv')

function exportToCSV(data) {
  const fields = ['uid', 'region', 'age', 'visitDays', 'totalPayment']
  const parser = new Parser({ fields })
  return parser.parse(data)
}
```

#### 4.3.2 반응형 디자인
**담당자**: 전체

**요구사항**
- 데스크톱, 태블릿, 모바일에서 정상 동작
- 최소 지원 해상도: 360px (모바일)
- 브라우저 호환: Chrome, Firefox, Safari, Edge

---

## 5. 비기능 요구사항

### 5.1 성능 요구사항
- **페이지 로드 시간**: 3초 이내
- **API 응답 시간**: 1초 이내
- **차트 렌더링 시간**: 2초 이내
- **동시 접속자**: 최대 10명 (학부 프로젝트 수준)

### 5.2 확장성
- 데이터 증가 시 MongoDB 인덱스로 대응
- 월별 데이터 추가 시 스키마 확장 가능

### 5.3 보안 (선택)
- 관리자 로그인 기능 (간단한 인증)
- API 키 기반 인증 (선택)

### 5.4 유지보수성
- 코드 주석 작성
- README 문서 작성
- Git 커밋 메시지 규칙 준수

---

## 6. 개발 일정

### 6.1 전체 일정 (15주)

| 주차 | 기간 | 담당 | 주요 업무 | 산출물 |
|------|------|------|-----------|--------|
| 1~2주 | Week 1-2 | 전체 | 주제 선정, 데이터 수집, 팀 구성 | 프로젝트 계획서 |
| 3~4주 | Week 3-4 | 전체 | 데이터 분석, 정제, 요구사항 분석 | 요구사항 명세서, 정제된 데이터 |
| 5~6주 | Week 5-6 | 전체 | 기술 스택 선정, 주요 변수 선정 | 시스템 설계서 |
| 7~8주 | Week 7-8 | 전체 | UI/UX 설계, 와이어프레임 | 화면 설계서 |
| 9~10주 | Week 9-10 | Backend | DB 구축, API 개발, 데이터 시각화 | API 문서, DB 스키마 |
| 11~12주 | Week 11-12 | Frontend | 대시보드 구현, KPI 화면 개발 | 프로토타입 v1.0 |
| 13~14주 | Week 13-14 | 전체 | 통합 테스트, 버그 수정 | 완성 프로그램 |
| 15주 | Week 15 | 전체 | 최종 발표 준비, 보고서 작성 | 최종 보고서, 발표자료 |

### 6.2 세부 일정

**Phase 1: 기획 및 분석 (1~4주)**
```
Week 1:
- [ ] 팀 구성 및 역할 분담
- [ ] 프로젝트 주제 확정
- [ ] 데이터셋 수집 및 분석

Week 2:
- [ ] 경쟁 사례 조사
- [ ] 유사 프로젝트 벤치마킹
- [ ] 프로젝트 목표 설정

Week 3:
- [ ] 데이터 품질 분석
- [ ] 데이터 정제 계획 수립
- [ ] 지역명 한글 변환 작업

Week 4:
- [ ] 기능 요구사항 정의
- [ ] 비기능 요구사항 정의
- [ ] 요구사항 명세서 작성
```

**Phase 2: 설계 (5~8주)**
```
Week 5:
- [ ] 시스템 아키텍처 설계
- [ ] DB 스키마 설계
- [ ] API 명세서 작성

Week 6:
- [ ] 주요 변수 선정 및 검증
- [ ] 통계 로직 설계
- [ ] 알고리즘 의사코드 작성

Week 7:
- [ ] 화면 구성도 작성
- [ ] 와이어프레임 제작
- [ ] 디자인 가이드 작성

Week 8:
- [ ] 개발 환경 구축
- [ ] Git 저장소 설정
- [ ] 프로젝트 구조 생성
```

**Phase 3: 개발 (9~12주)**
```
Week 9:
- [ ] MongoDB 설치 및 설정
- [ ] 데이터 import 스크립트 작성
- [ ] 인덱스 생성

Week 10:
- [ ] 백엔드 API 개발 (통계, 고객 조회)
- [ ] API 테스트 (Postman)
- [ ] 에러 핸들링 구현

Week 11:
- [ ] 프론트엔드 레이아웃 구현
- [ ] 차트 라이브러리 연동
- [ ] KPI 대시보드 구현

Week 12:
- [ ] 필터링 기능 구현
- [ ] 고객 상세 조회 구현
- [ ] Frontend-Backend 연동
```

**Phase 4: 테스트 및 마무리 (13~15주)**
```
Week 13:
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] 크로스 브라우저 테스트

Week 14:
- [ ] 버그 수정
- [ ] 성능 최적화
- [ ] 사용자 매뉴얼 작성

Week 15:
- [ ] 최종 보고서 작성
- [ ] 발표 자료 제작
- [ ] 발표 리허설
- [ ] 최종 발표
```

---

## 7. 역할 분담

### 7.1 팀 구성

| 이름 | 학번 | 역할 | 담당 기능 |
|------|------|------|-----------|
| 박선우 | 2021243096 | Frontend Developer | 월별 매출 추이, 지역/연령별 매출 분석 |
| 송재경 | 2021243112 | Frontend Developer | 고객 이용 비율 그래프 |
| 윤보석 | 2023243089 | Backend Developer | KPI 지표 API, 통계 계산 |
| 이태석 | 2021243071 | Full-stack Developer | 고객 조회 API, 필터링 기능 |

### 7.2 세부 역할

#### 박선우 (시각화 전문)
**주요 담당**
- 월별 매출 추이 그래프 구현
- 지역/연령대별 매출 분석 차트
- 출석률 시각화

**기술 스택**
- Chart.js 또는 Recharts
- React 컴포넌트 개발

**산출물**
- `SalesChart.jsx`
- `RegionAgeAnalysis.jsx`
- 차트 컴포넌트 문서

---

#### 송재경 (UI/UX 전문)
**주요 담당**
- 고객 이용 비율 파이 차트
- 전체 레이아웃 디자인
- 반응형 디자인

**기술 스택**
- Chart.js Pie Chart
- CSS/Tailwind

**산출물**
- `CustomerDistribution.jsx`
- `Layout.jsx`
- 디자인 가이드

---

#### 윤보석 (백엔드 로직)
**주요 담당**
- KPI 계산 로직 구현
- 통계 API 개발
- MongoDB Aggregation

**기술 스택**
- Node.js + Express
- MongoDB Aggregation Pipeline

**산출물**
- `/api/statistics`
- `/api/kpi`
- API 문서

---

#### 이태석 (전체 통합)
**주요 담당**
- 고객 조회 API
- 필터링 기능 (Backend + Frontend)
- 데이터 정제 스크립트
- 전체 통합 및 테스트

**기술 스택**
- Node.js + Express
- React
- Python (데이터 정제)

**산출물**
- `/api/customers`
- `/api/filter`
- `CustomerList.jsx`
- `FilterPanel.jsx`
- 데이터 정제 스크립트

---

### 7.3 공동 작업

**전체 팀원 참여**
- 요구사항 분석
- 시스템 설계
- 코드 리뷰
- 통합 테스트
- 최종 보고서 작성

**협업 도구**
- Git/GitHub (코드 관리)
- Discord/Slack (커뮤니케이션)
- Notion/Google Docs (문서 관리)
- Figma (디자인)

---

## 8. 기술 상세 명세

### 8.1 데이터베이스 스키마

```javascript
// MongoDB Schema (Mongoose)

const customerSchema = new Schema({
  uid: { 
    type: Number, 
    required: true, 
    unique: true,
    index: true 
  },
  region_city_group: { 
    type: String, 
    required: true,
    index: true 
  },
  region_city_group_no: { 
    type: Number 
  },
  region_city: { 
    type: String, 
    required: true,
    index: true 
  },
  age_group: { 
    type: String, 
    required: true,
    enum: ['Teens', 'Twenties', 'Thirties', 'Forties+'],
    index: true 
  },
  age: { 
    type: Number, 
    required: true,
    min: 10,
    max: 100 
  },
  visit_days: { 
    type: Number, 
    required: true,
    min: 0,
    max: 31 
  },
  total_duration_min: { 
    type: Number, 
    required: true,
    min: 0 
  },
  avg_duration_min: { 
    type: Number, 
    required: true,
    min: 0 
  },
  total_payment_may: { 
    type: Number, 
    required: true,
    min: 0,
    index: true 
  },
  retained_june: { 
    type: Number, 
    enum: [0, 1],
    default: 0 
  },
  retained_july: { 
    type: Number, 
    enum: [0, 1],
    default: 0 
  },
  retained_august: { 
    type: Number, 
    enum: [0, 1],
    default: 0 
  },
  retained_90: { 
    type: Number, 
    enum: [0, 1],
    default: 0,
    index: true 
  }
}, {
  timestamps: true // createdAt, updatedAt 자동 생성
})

// 복합 인덱스
customerSchema.index({ region_city_group: 1, age_group: 1 })
customerSchema.index({ total_payment_may: -1 })
```

### 8.2 API 명세서

#### 1) GET /api/statistics/kpi
**설명**: KPI 지표 조회  
**응답**:
```json
{
  "success": true,
  "data": {
    "totalRevenue": 1331828231,
    "totalCustomers": 22478,
    "arpu": 59250,
    "avgVisits": 4.24,
    "retentionRate": 46.26
  }
}
```

#### 2) GET /api/statistics/revenue-by-region
**설명**: 지역별 매출 통계  
**응답**:
```json
{
  "success": true,
  "data": [
    { "region": "서울", "revenue": 441716600, "customers": 8082 },
    { "region": "경기", "revenue": 350000000, "customers": 5779 }
  ]
}
```

#### 3) GET /api/customers
**설명**: 고객 목록 조회 (페이지네이션)  
**파라미터**:
- `page`: 페이지 번호 (default: 1)
- `limit`: 페이지당 개수 (default: 50)
- `sort`: 정렬 기준 (revenue, visits)

**응답**:
```json
{
  "success": true,
  "data": [...],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 22478,
    "totalPages": 450
  }
}
```

#### 4) GET /api/customers/:uid
**설명**: 특정 고객 상세 조회  
**응답**:
```json
{
  "success": true,
  "data": {
    "uid": 6626,
    "region": "서울",
    ...
  }
}
```

#### 5) POST /api/customers/filter
**설명**: 조건별 고객 필터링  
**요청 Body**:
```json
{
  "region": "서울",
  "ageGroup": "Twenties",
  "minPayment": 50000,
  "maxPayment": 200000,
  "retained90": 1
}
```

**응답**:
```json
{
  "success": true,
  "filters": {...},
  "data": [...],
  "statistics": {
    "count": 125,
    "totalRevenue": 15000000,
    "avgVisits": 5.2
  }
}
```

---

## 9. 테스트 계획

### 9.1 단위 테스트
- [ ] KPI 계산 로직 테스트
- [ ] 필터링 쿼리 테스트
- [ ] 데이터 정제 스크립트 테스트

### 9.2 통합 테스트
- [ ] API 엔드포인트 테스트
- [ ] Frontend-Backend 통신 테스트
- [ ] 차트 렌더링 테스트

### 9.3 성능 테스트
- [ ] 페이지 로드 시간 측정
- [ ] API 응답 시간 측정
- [ ] 대량 데이터 처리 테스트

### 9.4 사용자 테스트
- [ ] 관리자 시나리오 테스트
- [ ] 사용성 테스트
- [ ] 크로스 브라우저 테스트

---

## 10. 리스크 관리

### 10.1 기술적 리스크

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|-----------|
| MongoDB 연동 실패 | 중 | 높음 | 사전 학습, 대체 DB (MySQL) 고려 |
| 차트 라이브러리 호환성 | 중 | 중 | 여러 라이브러리 테스트 |
| 데이터 정제 오류 | 높음 | 높음 | 충분한 검증 시간 확보 |
| 성능 이슈 | 낮음 | 중 | 인덱스 최적화, 캐싱 |

### 10.2 일정 리스크

| 리스크 | 확률 | 대응 방안 |
|--------|------|-----------|
| 기능 구현 지연 | 중 | 우선순위 조정, 선택 기능 축소 |
| 팀원 개인 사정 | 중 | 역할 교차 학습, 백업 플랜 |
| 중간고사/기말고사 기간 | 높음 | 사전 진행 또는 일정 조정 |

---

## 11. 성공 기준

### 11.1 필수 기능 (Must Have)
- [ ] 대시보드 페이지 구현
- [ ] 최소 3개 이상의 차트 구현
- [ ] KPI 지표 표시
- [ ] 고객 목록 조회
- [ ] 지역/연령대 필터링

### 11.2 권장 기능 (Should Have)
- [ ] 고객 상세 조회
- [ ] 페이지네이션
- [ ] 반응형 디자인
- [ ] 데이터 내보내기 (CSV)

### 11.3 선택 기능 (Nice to Have)
- [ ] 관리자 로그인
- [ ] 실시간 업데이트
- [ ] 다크 모드
- [ ] 다국어 지원

---

## 12. 참고 자료

### 12.1 기술 문서
- MongoDB 공식 문서: https://docs.mongodb.com/
- Express.js 가이드: https://expressjs.com/
- React 공식 문서: https://react.dev/
- Chart.js: https://www.chartjs.org/

### 12.2 참고 프로젝트
- (유사 대시보드 프로젝트 링크)
- (데이터 시각화 예제)

---

## 부록

### A. 데이터 샘플

```csv
uid,region_city_group,region_city,age_group,age,visit_days,total_payment_may
11,Seoul,Seoul,Twenties,23,1,13000
705,Gyeonggi-do,Yongin,Twenties,27,5,39000
1754,Chungcheongnam-do,Cheonan,Thirties,38,5,45000
```

### B. 환경 설정 가이드

```bash
# Backend 설정
cd backend
npm install
cp .env.example .env
# .env 파일 수정 (MongoDB URI 등)
npm run dev

# Frontend 설정
cd frontend
npm install
npm start
```

### C. Git 커밋 컨벤션

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 코드 리팩토링
test: 테스트 코드
chore: 빌드 설정 등

예시:
feat: Add KPI dashboard component
fix: Fix pagination bug in customer list
docs: Update API documentation
```

---

**문서 버전**: 1.0  
**최종 수정일**: 2024년  
**작성자**: 웹프레임워크백엔드 2조

